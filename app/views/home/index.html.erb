<h1>Home</h1>


<h2>Agenda:</h2>

<table>
  <thead>
    <tr>
      <th>Horário</th>            
      <% unique_days = unique_days(@groups) %>
      <% unique_days.each do |day| %>
        <th><%= day %></th>
      <% end %>
    </tr>
  </thead>

  <tbody>
    <% sorted_time = sorted_times(@groups) %>
    <% sorted_time.each do |time| %>
      <tr>
        <td><%= time %></td>
        <% unique_days.each do |day| %>
          <% group = find_group(@groups, time, day) %>
          <td>
            <%= format_group_cell(group) %>
          </td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<h2>TODO LIST:</h2>
<div>
  <ul>
    <li>
      Desenvolver lógica para fases de treinamento e grupos tecnicos nos planos anuais
    </li>
    <li>
      Sistema de vendas
    </li>
    <li>
      Tornar planos em produtos
    </li>
  </ul>
</div>
